# ─── Face Detection ───────────────────────────────────────────────────────────
# Primary (best accuracy, requires cmake + C++ build tools)
face-recognition>=1.3.0
dlib>=19.22.0

# OpenCV fallback (install if face-recognition fails)
opencv-python>=4.8.0

# Image processing (PIL)
Pillow>=10.0.0

# Numpy (shared by both face_recognition and OpenCV)
numpy>=1.24.0

# ─── Web / HTTP ───────────────────────────────────────────────────────────────
requests>=2.31.0

# ─── Blockchain ───────────────────────────────────────────────────────────────
web3>=6.11.0

# ─── Environment / CLI ────────────────────────────────────────────────────────
python-dotenv>=1.0.0
